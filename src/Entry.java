import java.util.Scanner;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Entry {
    public void Entry(){
    Scanner scanner=new Scanner(System.in);
    String vorudi="";

        while (true) {
            vorudi = scanner.nextLine();

            Pattern pattern_vorudi_voltagedc = Pattern.compile("");
            Matcher matcher_vorudi_voltagedc = pattern_vorudi_voltagedc.matcher(vorudi);
            if (matcher_vorudi_voltagedc.find()){

            }

            Pattern pattern_vorudi_voltageac = Pattern.compile("");
            Matcher matcher_vorudi_voltageac = pattern_vorudi_voltageac.matcher(vorudi);
            if (matcher_vorudi_voltageac.find()){

            }

            Pattern pattern_vorudi_voltagedependentvoltage = Pattern.compile("");
            Matcher matcher_vorudi_voltagedependentvoltage = pattern_vorudi_voltagedependentvoltage.matcher(vorudi);
            if (matcher_vorudi_voltagedependentvoltage.find()){

            }

            Pattern pattern_vorudi_voltagedependentcurrent = Pattern.compile("");
            Matcher matcher_vorudi_voltagedependentcurrent = pattern_vorudi_voltagedependentcurrent.matcher(vorudi);
            if (matcher_vorudi_voltagedependentcurrent.find()){

            }

            Pattern pattern_vorudi_currentdependentvoltage = Pattern.compile("");
            Matcher matcher_vorudi_currentdependentvoltage = pattern_vorudi_currentdependentvoltage.matcher(vorudi);
            if (matcher_vorudi_currentdependentvoltage.find()){

            }

            Pattern pattern_vorudi_currentdependentcurrent = Pattern.compile("");
            Matcher matcher_vorudi_currentdependentcurrent = pattern_vorudi_currentdependentcurrent.matcher(vorudi);
            if (matcher_vorudi_currentdependentcurrent.find()){

            }

            Pattern pattern_vorudi_currentdc = Pattern.compile("");
            Matcher matcher_vorudi_currentdc = pattern_vorudi_currentdc.matcher(vorudi);
            if (matcher_vorudi_currentdc.find()){

            }

            Pattern pattern_vorudi_currentac = Pattern.compile("");
            Matcher matcher_vorudi_currentac = pattern_vorudi_currentac.matcher(vorudi);
            if (matcher_vorudi_currentac.find()){

            }

            Pattern pattern_vorudi_resistor = Pattern.compile("");
            Matcher matcher_vorudi_resistor = pattern_vorudi_resistor.matcher(vorudi);
            if (matcher_vorudi_resistor.find()){

            }

            Pattern pattern_vorudi_capacitor = Pattern.compile("");
            Matcher matcher_vorudi_capacitor = pattern_vorudi_capacitor.matcher(vorudi);
            if (matcher_vorudi_capacitor.find()){

            }

            Pattern pattern_vorudi_self = Pattern.compile("");
            Matcher matcher_vorudi_self = pattern_vorudi_self.matcher(vorudi);
            if (matcher_vorudi_self.find()){

            }

            Pattern pattern_vorudi_etmam = Pattern.compile("");
            Matcher matcher_vorudi_etmam = pattern_vorudi_etmam.matcher(vorudi);
            if (matcher_vorudi_etmam.find()){

            }
        }

    }
}
